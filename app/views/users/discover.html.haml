= render :partial => "users/usersidebox"
.mainbox
  .formbox
    .title
      发表
    .nav
      %ul
        %li= link_to "分享", share_users_url
        %li= link_to "发现", discover_users_url, :class => "on"
        .clearfix
    .formstyle
      = form_tag(updatesetting_users_url, :multipart => true) do
        %table{:width => '100%', :cellspadding => 6}
          %tr
            %td{:width => 100} 
            %td
          %tr
            %td{:width => 100} 
            %td
        .btn
          = submit_tag "确定", :class => "submit"
        
.clearfix